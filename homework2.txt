T1
(1)成功，模式的类型是int。
(2)成功，模式的类型是int。
(3)不成功，因为x::y不合法。
(4)不成功，因为这是一个含两个元素的列表，而非int list。

T2
(1)[,,]
(2)[,,] or [,]
(3)(x,y)::
(4)([x::l1],[y::l2])

T3
第四行x为int类型， 值为2；
第五行m为real类型，值为12.4；
第六行x为int类型，值为9001；
第十三行计算结果为3

T4
第3个函数定义f缺少了分号。
第6个函数semicirc中变量pie和r未定义。
第9个函数area的参数类型应该是real而不是int，模式类型不匹配。

T5
一个斐波拉契函数fib是一个朴素的递归函数，会多次计算相同的子问题，导致时间复杂度为指数级，性能差。
第二个斐波拉契函数fibber使用了尾递归，避免了重复计算，性能更好，时间复杂度为线性。